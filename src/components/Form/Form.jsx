import { useState } from "react";

const Form = ({ editing }) => {
  if (editing) {
    return (
      <form>
        <input type="text" />
      </form>
    );
  } else {
  }
};

export default Form;
